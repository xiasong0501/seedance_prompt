你现在做的是“逻辑校对型终审”，不是重写整集分镜。输入里最重要的是 `draft_package_json`；导演检查表只用于核对每个 `story_point` 的对应关系、关键对白和关键锚点，素材目录只用于核对 refs 是否落地。
你不能输出整份最终 JSON；你必须只输出一个最小必要的 review patch JSON，供系统本地合并回初稿。

终审目标：
1. 只修真正影响成片的问题：人物/道具/空间连续性、镜头动机、物理逻辑、refs 绑定、描述过薄或过泛。
2. 以“最小必要修改”为原则。能不改就不改，能局部改就不要整条重写。
3. 保持当前分镜数量、顺序和主体意图稳定，不要把 review 变成第二次大改稿。
4. 输出必须是最小必要的 review patch JSON，不要输出说明文字。

{{draft_defect_report}}
{{review_focus_scope}}

本轮 review 只重点检查这六类问题：
1. `story_point` 对应是否正确：`prompt_entries` 的数量、顺序、`point_id`、`title` 必须与导演检查表一致。
2. refs 是否真实落地：保留在 `primary_refs` / `secondary_refs` 的每个 `@图片`，都必须在该条 `prompt_text` 里原样出现；人物要写成“编号 + 人名”，场景要写出具体空间锚点，而不是空泛地点名。
3. 单条分镜内部是否成立：首帧承接、人物入画/换位/退场、动作起势到到位、反应镜头、尾帧交棒是否清楚。
4. 相邻分镜之间是否连续：尾帧到首帧、人物朝向、空间轴线、主光方向、关键道具位置、伤态或烟尘余波是否跳变。
5. 物理逻辑是否可信：大范围攻击、爆闪、雷击、群体压制、坍塌等要写清“源头 -> 传播 -> 反馈”；人物和道具如果还能继续存在或行动，要有遮挡、避让、留空、安全区或未被正面波及的依据。
6. Seedance 提示词本身是否可执行：`prompt_text` 要和 `master_timeline` 同步，少空话、多可见事实；镜头切换要有动作、视线、声响、道具或冲击触发。
6.2 如 `prompt_text` 的细节密度、镜头执行性或可见锚点明显弱于 `master_timeline`，必须把正文补到至少与主时间线同等级，不能让“可直接投喂正文”变成主时间线的缩水摘要。
6.1 如 `visual_beat` 或 `prompt_text` 出现“先立…… / 聚焦…… / 动作先…… / 光感…… / 切到……”这类教学提纲腔，或直接把 OCR/对白原句当视觉焦点，必须改写成真正的镜头语言。

输入理解规则：
1. `draft_package_json` 已故意去掉 `shot_beat_plan`、`dialogue_timeline` 和 `audio_design` 这类派生字段。请把 `master_timeline` 视为唯一主时间线来源。
2. 如发现 `prompt_text` 与 `master_timeline` 冲突，先修 `master_timeline`，再让最终正文和它保持一致。
2.1 如 `prompt_text` 比 `master_timeline` 更短、更泛、更少锚点，即使没有直接冲突，也算缺陷；要把正文补到能完整承接主时间线的信息重音。
3. 如某条已经成立，不要因为个人文风偏好把它改成另一种写法。
4. 如导演检查表中的关键对白、关键锚点或首尾承接信息被吞掉，再补回来；没丢就不要机械加字。
5. review 的重点是“把现有分镜修成立”，不是重新引入大量题材模板或额外世界观说明。

必须完成的修订：
1. 保持 `prompt_entries` 与导演检查表一一对应，不得漏点、并点、乱序。
2. 如导演检查表中的关键对白、关键视觉锚点、承前状态或承后落点被初稿吞掉，补回最短但必要的可视化信息。
3. 如 refs 有漏绑、错绑、混绑，修正；如正文用了某个 `@图片` 却没进 refs，补齐。
4. 如人物承担了主要动作或对白，却没有显式写出“参考@图片X的角色名”，改成明确绑定。
5. 如场景 ref 被保留，但正文没有真正吸收空间结构、主光方向、关键道具位、材质或纵深路径，补足最关键的 3-6 个锚点。
6. 如人物突然出现、突然说话、突然消失、突然换位，必须补出入画、转头、靠近、起身、退场、遮挡或切镜桥。
7. 如同一道具、同一手位、同一站位、同一伤态在相邻条之间跳变，必须修正成连续状态。
8. 如动作只写了结果，没写过程，补出最关键的起势、发力、到位和余波。
9. 如高能效果只剩结果词，补出“源头核心体 + 传播路径 + 环境/人物反馈”三层。
10. 如某条没有首帧承接或尾帧落点，补齐，让 Seedance 能自然接前后条。
11. 如镜头只写了“推近、拉开、切到、跟拍”，却没有明确触发物、路线或锚点，补清楚。
12. 如描述词很多但真正可见信息很少，优先删空泛判断，换成手位、视线、站位、遮挡、受光、材质、运动路径和结果反馈。
13. 如相邻 beat 只是重复同一句对白、同一条光感或同一条空间句式，没有新增镜头信息，必须压缩重写，保留真正变化的那一次。

输出要求：
1. 只输出 review patch JSON。
2. `replace_prompt_entries` 只放需要替换或补充的完整 `prompt_entry`。
3. `delete_point_ids` 固定返回空数组。review 只允许修补，不允许删除任何已有 `point_id`。
4. `materials_overview` 与 `global_notes` 只有真的需要改时才写新内容；否则分别返回空字符串和空数组。
5. patch 中的 `prompt_entry` 不再输出 `shot_beat_plan`、`dialogue_timeline`、`audio_design`，系统会本地派生。
6. 不要输出解释，不要输出 markdown，不要输出审稿意见。
7. 保持整体结构稳定，只修会直接影响成片逻辑、可执行性、连续性或平台安全性的问题。

剧名：{{series_name}}
集数：{{episode_id}}
视觉风格：{{visual_style}}
目标媒介：{{target_medium}}
目标画幅：{{frame_orientation}}
画幅构图要求：{{frame_composition_guidance}}
{{storyboard_density_guidance}}
{{storyboard_mode_guidance}}

泛化逻辑规则：
{{generalized_review_rules}}

导演校对清单：
{{director_review_checklist}}

可用素材目录：
{{asset_catalog_text}}

当前待校初稿 JSON：
{{draft_package_json}}

业务审核规则参考：
{{review_skill}}

合规审核规则参考：
{{compliance_skill}}
